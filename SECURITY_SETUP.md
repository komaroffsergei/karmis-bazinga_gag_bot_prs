# Local credentials

Set `MEGA_EMAIL` and `MEGA_PASSWORD` as environment variables before running main.py. For PostgreSQL, set DB_* environment variables or copy `database.ini.example` to local `database.ini` and fill it. `.env.example` lists names only; this project does not automatically load .env. Never commit the local credential files. Historical credentials were removed from every branch/tag.
